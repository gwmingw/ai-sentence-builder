# Design Guide

이 문서는 `AI Sentence Builder Booth Demo`의 현재 웹 UI와 디자인 기준을 정리한 문서입니다. 대상 사용자는 초등학생이며, 사용 환경은 짧은 시간 안에 여러 명이 돌아가며 체험하는 오프라인 부스입니다.

핵심 목표는 “예쁘지만 방해하지 않는 화면”입니다. 학생은 설명을 오래 읽지 않고도 시작 문장, 현재 문장, AI 후보, 조작 버튼을 바로 이해할 수 있어야 합니다.

## 디자인 목표

- 초등학생에게 친근하고 밝은 인상을 준다.
- 운영자가 옆에서 빠르게 안내할 수 있도록 화면 흐름을 단순하게 유지한다.
- AI가 하나의 정답을 내는 것이 아니라 여러 후보를 제안한다는 점을 카드 UI와 점수 막대로 보여준다.
- 모바일, 태블릿, 노트북, 빔프로젝터 환경에서 모두 글자가 잘리지 않게 한다.
- 부스 회전률을 위해 체험 시작, 후보 선택, 되돌리기, 초기화가 모두 빠르게 가능해야 한다.

## 현재 분위기

키워드:

- 파스텔
- 밝은 교실
- 이야기 만들기
- 귀엽지만 과하지 않은 AI 실험실
- 초등학생용 체험 도구

피해야 할 방향:

- 어두운 SF 스타일
- 복잡한 대시보드
- 설명이 긴 학습 자료형 화면
- 장식이 너무 많은 게임 UI
- 웹폰트 의존도가 높은 불안정한 화면

## 컬러

현재 CSS 변수:

```css
:root {
  --bg-main: #fff8ed;
  --bg-panel: #ffffff;
  --text-main: #2f2a3a;
  --text-soft: #6b6378;
  --primary: #ff8fab;
  --primary-strong: #f45b88;
  --mint: #a8e6cf;
  --sky: #a7d8ff;
  --lemon: #ffe680;
  --lavender: #d7c4ff;
  --peach: #ffc6a5;
  --border-soft: #f1d9cc;
  --shadow-soft: rgba(80, 60, 40, 0.12);
}
```

사용 원칙:

- 배경은 따뜻한 크림/분홍 계열을 기본으로 한다.
- 핵심 액션과 상태 강조에는 분홍 계열 `--primary`, `--primary-strong`을 사용한다.
- 후보 카드는 `mint`, `sky`, `lemon`, `lavender`, `peach`를 번갈아 사용해 여러 갈래의 가능성을 시각화한다.
- AI 점수 막대는 잘 보여야 하므로 분홍-주황-노랑 계열 그라데이션을 사용한다.
- 문장 진행도 막대는 보조 정보이므로 회색에 가까운 낮은 대비 색상을 사용한다.

## 타이포그래피

현재는 외부 웹폰트를 사용하지 않고 안정적인 기본 한글 폰트 스택을 사용한다.

```css
font-family:
  "Pretendard",
  "Noto Sans KR",
  "Apple SD Gothic Neo",
  "Malgun Gothic",
  system-ui,
  sans-serif;
```

굵기 기준:

- 기본 본문: `400`
- 제목, 버튼, 배지, 후보 문장, 완성 문장: `700`
- `800`, `900`처럼 과한 굵기는 사용하지 않는다.

크기 기준:

- 메인 제목: `clamp(30px, 5vw, 54px)`
- 현재 문장: `clamp(24px, 3.2vw, 34px)`
- 체험 시작 후 현재 문장: `clamp(24px, 2.7vw, 30px)`
- 후보 카드 문장: `clamp(20px, 2.2vw, 27px)`
- 완성 화면 제목: `clamp(34px, 5vw, 58px)`
- 완성 이야기 줄: `clamp(23px, 3.2vw, 34px)`

주의:

- viewport 너비에 따라 폰트를 직접 비례 확대하지 않는다.
- 글자 간격은 `0`을 유지한다.
- 긴 문장은 `overflow-wrap: anywhere`로 부모 영역 밖으로 밀리지 않게 한다.

## 화면 구조

현재 화면은 다음 영역으로 구성된다.

```text
상단
  AI Sentence Builder / 제목 / 현재 후보 출처 상태 배지(#sourceLabel)

현재 문장 영역
  지금까지 만든 문장
  실제 문장
  0 / 5 진행도

조작 영역
  상상력 슬라이더
  다시 추천 / 되돌리기 / AI가 하나 골라줘 / 처음부터

시작 문장 영역
  체험 전만 표시
  체험 시작 후 완전히 숨김

AI 후보 영역
  후보 카드 6개
  AI 점수, 분위기 태그, 짧은 이유

완성 화면
  5단계 완료 후 다른 영역을 숨기고 완성 이야기만 표시
```

## 체험 전 화면

체험 전에는 시작 문장 입력 영역과 예시 문장 버튼이 보인다.

원칙:

- 랜딩 페이지처럼 설명을 길게 넣지 않는다.
- 학생이 바로 입력하거나 예시를 눌러 시작할 수 있게 한다.
- 시작 문장 입력은 한 줄 중심으로 간결하게 유지한다.

체험이 시작되면 `.starter-zone`은 완전히 숨긴다. 체험 중에는 현재 문장과 AI 후보를 함께 보는 것이 더 중요하다.

## 현재 문장 영역

현재 문장 영역은 체험의 중심이다.

관련 구현:

- 영역 클래스: `.story-stage`
- 제목 ID: `#storyTitle`
- 실제 문장 ID: `#storyText`
- 문장 추가 애니메이션 클래스: `.story-text-pop`
- 진행도 막대: `#progressFill`
- 진행도 텍스트: `#progressLabel`

구현 기준:

- 체험 전에는 “시작 문장을 골라볼까요?” 역할을 한다.
- 체험 후에는 “지금까지 만든 문장” 라벨을 작게 표시한다.
- 실제 문장은 더 크게 보이며 분홍색 밑줄 강조를 가진다.
- 후보가 선택되어 문장이 추가될 때 `story-pop` 애니메이션으로 짧게 강조된다.
- 5단계 완료 후에는 이 영역을 숨기고 완성 화면으로 전환한다.

주의:

- “지금까지 만든 문장” 라벨이 실제 문장과 시각적으로 경쟁하면 안 된다.
- 현재 문장 영역은 너무 높아지지 않게 유지한다.
- 진행도 `0 / 5`는 줄바꿈되지 않도록 `white-space: nowrap`을 유지한다.

## 상태 배지

상단 오른쪽의 상태 배지는 현재 후보가 어디서 왔는지 운영자와 학생이 빠르게 알 수 있게 하는 작은 안내 장치다.

관련 구현:

- ID: `#sourceLabel`
- 클래스: `.status-pill`

표시 방향:

- 초기 상태: 준비 완료
- Solar API 응답: Solar 후보 또는 AI 후보 생성 완료를 의미하는 문구
- 캐시 응답: 방금 만든 후보를 다시 보여준다는 의미의 문구
- fallback 응답: 준비된 후보 또는 임시 후보를 사용한다는 의미의 문구

주의:

- 상태 배지는 너무 길면 모바일에서 잘리므로 짧게 유지한다.
- 개발자용 오류 메시지를 그대로 보여주지 않는다.
- `fallback`은 실패처럼 보이기보다 체험이 계속된다는 느낌으로 표현한다.

## 조작 영역

조작 영역은 부스 운영자가 자주 만지는 영역이다.

현재 구성:

- 상상력 슬라이더
- 다시 추천
- 되돌리기
- AI가 하나 골라줘
- 처음부터

데스크톱:

- `.controls`는 `grid-template-columns: minmax(420px, 1fr) max-content`를 사용한다.
- 왼쪽 슬라이더 영역이 남는 공간을 가져가고, 오른쪽 버튼 묶음은 필요한 만큼만 차지한다.
- 화면 상단에 sticky로 붙어 현재 문장 바로 아래에서 접근하기 쉽게 유지한다.

모바일:

- 조작 영역은 하단 fixed bar로 유지한다.
- 버튼은 여러 줄로 자연스럽게 감긴다.
- 본문이 하단 바에 가려지지 않도록 `body.has-story .app-shell`에 충분한 하단 padding을 둔다.

주의:

- 조작 버튼이 후보 카드보다 너무 멀리 떨어지면 안 된다.
- 모바일에서는 세로 배치를 유지해 터치 실수를 줄인다.
- API 호출 중에는 중복 클릭을 막는다.

## 상상력 슬라이더

상상력 슬라이더는 내부적으로 temperature 값을 조절한다.

표시 문구:

```text
차분하게 | 보통 | 엉뚱하게
```

원칙:

- 학생에게 “temperature”라고 설명하지 않는다.
- 슬라이더는 조작 도구처럼 충분히 길게 보여야 한다.
- 데스크톱에서는 버튼과 슬라이더 사이의 빈 공간이 과하게 생기지 않도록 왼쪽 영역을 넓게 사용한다.

## 후보 카드

후보 카드는 학생이 가장 많이 상호작용하는 요소다.

관련 구현:

- 후보 영역: `.candidate-zone`
- 후보 그리드: `.candidate-grid`
- 후보 카드: `button.candidate-card`
- 카드 skeleton: `.candidate-card.skeleton`
- AI 점수 막대: `.score-track`

현재 기준:

- 기본 후보 수는 6개다.
- 데스크톱: 3 x 2 그리드
- 태블릿: 2열 그리드
- 모바일: 1열 6개
- 카드 radius는 `8px`
- 카드 안에는 후보 문장, 분위기 태그, 짧은 이유, AI 점수 막대가 들어간다.

AI 점수:

- 실제 모델 내부 확률이 아니다.
- 학생 체험용 상대 점수다.
- UI에서는 점수 막대를 눈에 잘 띄게 보여준다.
- 막대는 분홍-주황-노랑 계열 그라데이션을 사용한다.

주의:

- 후보 문장은 현재 문장 뒤에 바로 붙는 형태여야 한다.
- 후보 카드 텍스트가 너무 길면 체험 리듬이 느려진다.
- 카드 전체를 클릭 가능한 버튼처럼 인식할 수 있어야 한다.
- 후보 카드는 실제로 `button` 요소여야 한다. 키보드 접근성과 클릭 가능성을 보장하기 위해 단순 `div` 카드로 만들지 않는다.
- 카드 내부 텍스트가 길어져도 카드 밖으로 넘치지 않아야 한다.

## 로딩 상태

로딩은 후보 영역 안에서만 표시한다.

관련 구현:

- 로딩 메시지 클래스: `.candidate-loading`
- skeleton 카드 클래스: `.candidate-card.skeleton`
- 로딩 점 애니메이션: `.dot-row`

현재 구현:

- 화면 전체 blur overlay는 사용하지 않는다.
- 후보 카드 영역 안에 “AI가 다음 말을 생각하고 있어요” 메시지를 표시한다.
- 작은 점 애니메이션과 skeleton 카드를 함께 보여준다.

이유:

- 문장이 추가되는 애니메이션이 사용자에게 보여야 한다.
- 화면 전체가 흐려지면 현재 문장 변화와 후보 영역의 관계를 놓치기 쉽다.

## 자동 스크롤

후보 생성 후 자동 스크롤은 후보 섹션만 중앙에 두지 않는다. 현재 문장, 조작 영역, 후보 카드가 함께 보이는 위치로 이동해야 한다.

원칙:

- 학생은 “지금까지 만든 문장”과 “AI가 생각한 다음 말”을 동시에 봐야 한다.
- 데스크톱에서는 현재 문장과 조작 버튼이 함께 보여야 한다.
- 모바일에서는 하단 fixed 조작 바를 고려해 후보 카드가 가려지지 않게 한다.

## 완성 화면

5단계가 끝나면 체험 결과에 집중한다.

관련 구현:

- 완료 영역: `#finishPanel`, `.finish-panel`
- 완료 배지: `.finish-badge`
- 완성 이야기 컨테이너: `#finalStory`, `.final-story`
- 완성 이야기 한 줄: `.final-story-line`
- 다시 만들기 버튼: `#finishResetButton`

현재 구현:

- 상단, 현재 문장 영역, 조작 영역, 시작 문장 영역, 후보 영역을 모두 숨긴다.
- 완성 패널만 화면에 표시한다.
- 이야기는 선택 단계별 문장 조각을 줄바꿈해 보여준다.
- 숫자 목록은 사용하지 않는다.
- 각 줄은 순차적으로 나타나는 애니메이션을 가진다.
- “다시 만들기” 버튼으로 다음 학생 체험을 시작할 수 있다.

원칙:

- 완성 이야기는 학생에게 결과물처럼 보여야 한다.
- 한 줄로 길게 나열하지 않는다.
- 너무 많은 장식보다 읽기 좋은 문장 덩어리를 우선한다.

## 반응형 기준

주요 breakpoint:

- `900px` 이하: 후보 카드 2열, 조작 영역 1열
- `640px` 이하: 모바일 레이아웃, 후보 카드 1열, 조작 영역 하단 fixed

모바일 주의사항:

- 제목과 상태 배지가 잘리지 않아야 한다.
- 진행도 `0 / 5`가 줄바꿈되지 않아야 한다.
- 후보 카드는 1열로 읽기 쉽게 쌓는다.
- 하단 조작 바 때문에 콘텐츠가 가려지지 않게 padding을 확보한다.
- 버튼은 최소 42px 이상의 터치 높이를 가진다.

## 접근성 및 사용성

- 모든 주요 버튼은 키보드로 접근 가능해야 한다.
- 상태 문구에는 `aria-live`를 사용한다.
- 로딩 메시지는 후보 영역 내부에서 `role="status"`로 전달한다.
- 색상만으로 점수를 구분하지 않고 숫자와 막대를 함께 제공한다.
- 긴 문장은 줄바꿈되어도 카드 밖으로 삐져나가지 않아야 한다.
- 버튼, 입력창, 카드의 클릭 영역은 충분히 커야 한다.

## 부스 운영 기준

- 한 체험은 1~2분 안에 끝나는 것을 목표로 한다.
- 기본 선택 횟수는 5단계로 제한한다.
- 기본 후보 수는 6개로 유지한다.
- 오류가 나도 체험이 멈추지 않도록 fallback 후보를 보여준다.
- 운영자는 `처음부터`와 완료 화면의 `다시 만들기`로 빠르게 다음 학생에게 넘길 수 있어야 한다.
- 화면에 긴 설명을 넣지 말고, 필요한 설명은 운영자가 말로 보충한다.

## 자동 초기화

부스에서는 한 학생이 체험을 마친 뒤 화면이 그대로 방치될 수 있으므로 자동 초기화가 필요하다.

관련 구현:

- 사용자 활동 시간: `state.lastActivity`
- 주기 확인 함수: `checkIdleReset`
- 활동 갱신 함수: `markActivity`

디자인 기준:

- 자동 초기화는 화면에 큰 경고를 띄우지 않고 조용히 처음 상태로 돌아가는 편이 좋다.
- 운영자가 직접 넘길 때는 `처음부터` 또는 `다시 만들기` 버튼을 사용한다.
- 자동 초기화가 있더라도 버튼을 통한 수동 초기화가 항상 보여야 한다.
- 체험 중 실수로 초기화되는 느낌이 들지 않도록 idle 기준 시간은 너무 짧게 잡지 않는다.

## 금지 또는 주의사항

- 실제 logits 또는 내부 확률처럼 오해될 문구를 사용하지 않는다.
- 화면 전체 blur loading overlay를 다시 넣지 않는다.
- 시작 후 시작 문장 입력 영역을 다시 노출하지 않는다.
- 완성 화면에서 후보 카드나 조작 영역을 함께 보여주지 않는다.
- 카드 안에 또 다른 카드 스타일을 중첩하지 않는다.
- 지나치게 둥근 pill/card 남용을 피한다. 기본 radius는 `8px`를 유지한다.
- 웹폰트를 다시 도입할 경우 현장 네트워크와 가독성을 먼저 확인한다.
- 한 가지 색조만 반복해 화면이 단조로워지지 않게 한다.

## 최종 확인 체크리스트

- 체험 전 화면에서 무엇을 누르면 되는지 바로 보이는가
- 체험 시작 후 시작 문장 영역이 완전히 사라지는가
- 현재 문장과 후보 카드가 동시에 보이는가
- 후보 카드가 데스크톱 3 x 2, 모바일 1열로 보이는가
- AI 점수 막대가 충분히 눈에 띄는가
- 진행도 막대가 과하게 강조되지 않는가
- 로딩이 후보 영역 안에서만 보이는가
- 문장 추가 애니메이션이 가려지지 않는가
- 5단계 완료 후 완성 화면만 예쁘게 보이는가
- 모바일에서 하단 조작 바가 콘텐츠를 가리지 않는가
